# System

Linux kernel, NVIDIA drivers, and CVM image.

- **`configs/`** — Linux Kernel configuration files
- **`cvmimage/`** — CVM image build (mkosi)
- **`noble/`** — Kernel source (clone on build)
- **`open-gpu-kernel-modules/`** — NVIDIA open kernel modules
- **`scripts/`** — Build scripts

